# Ansible task - Install nginx on pathnex server

---
- name: Install nginx on pathnex server
  hosts: all
  become: yes

  tasks:
    - name: Install Nginx
      yum: 
        name: nginx
        state: present

# Docker
docker --version
docker run ubuntu echo "Hello Pahtnex"

# docker file
FROM ubuntu:22.04
CMD["echo", "Hello Pahtnex"]